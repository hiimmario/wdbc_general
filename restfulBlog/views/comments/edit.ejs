<% include ../partials/header %>

<div class="ui main text container segment">
    <div class="ui huge header">Edit Comment</div>

    <form class="ui form" action="/blogs/<%= blogId %>/comments/<%= comment._id %>?_method=PUT" method="POST">
        <div class="field">
            <label>Comment</label>
            <textarea name="comment[text]"><%= comment.text %></textarea>
        </div>
        <input class="ui primary button" type="submit">
    </form>
</div>

<% include ../partials/footer %>